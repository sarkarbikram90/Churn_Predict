# Churn_Predict
End-to-End ML pipeline using MLflow for experiment tracking and model registry, trained on real data, with artifacts logged to AWS S3. The model can be promoted, versioned, and served. This mirrors how we’d run production ML.
